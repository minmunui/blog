---
trigger: always_on
---

Prompt 3: 콘텐츠 로직 (Collections & Metadata)
목적: 블로그, 포트폴리오, 이력서의 데이터를 분류하고 관리하는 기능을 만듭니다.

"Eleventy의 Collections 기능을 사용하여 콘텐츠를 자동 분류하는 로직을 작성해 줘.

블로그: posts 폴더의 MD 파일을 날짜 내림차순으로 정렬하고, 태그(tags)와 카테고리(categories)별로 묶어줘.

포트폴리오: portfolio 폴더의 데이터를 읽어와서 목록을 생성해 줘.

이력서: resume.md 파일 하나만 수정해도 이력서 페이지가 업데이트되도록 레이아웃을 연결해 줘.

모든 포스트의 Front Matter(상단 정보)에는 title, date, tags, category, description이 포함되도록 규격화해 줘."