---
layout: layouts/base.njk
title: Portfolio
---

<div class="container max-w-6xl py-6 lg:py-10">
  <div class="flex flex-col items-start gap-4 md:flex-row md:justify-between md:gap-8">
    <div class="flex-1 space-y-4">
      <h1 class="inline-block font-extrabold text-4xl tracking-tight lg:text-5xl">Portfolio</h1>
      <p class="text-xl text-muted-foreground">
        Showcase of my projects and experiments.
      </p>
    </div>
  </div>
  <hr class="my-8" />
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {% for project in collections.portfolio | reverse %}
    <a href="{{ project.url }}" class="group relative flex flex-col overflow-hidden rounded-xl border bg-card text-card-foreground shadow-sm transition-all hover:shadow-md hover:border-primary/50">
        <div class="aspect-video w-full overflow-hidden border-b bg-muted">
             {% if project.data.image %}
                <img src="{{ project.data.image }}" alt="{{ project.data.title }}" class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" />
             {% else %}
                <div class="flex h-full w-full items-center justify-center text-muted-foreground bg-secondary/50">
                    <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="14" x="2" y="3" rx="2"/><line x1="8" x2="16" y1="21" y2="21"/><line x1="12" x2="12" y1="17" y2="21"/></svg>
                </div>
             {% endif %}
        </div>
        <div class="flex flex-col space-y-2 p-4">
            <h3 class="text-xl font-bold leading-none tracking-tight group-hover:text-primary transition-colors">{{ project.data.title }}</h3>
            <p class="text-sm text-muted-foreground line-clamp-2">{{ project.data.description }}</p>
            <div class="mt-2 flex flex-wrap gap-2">
                {% for tech in project.data.tech %}
                    <span class="inline-flex items-center rounded-sm border px-2 py-0.5 text-xs font-semibold text-foreground transition-colors bg-secondary/50">{{ tech }}</span>
                {% endfor %}
            </div>
        </div>
    </a>
    {% endfor %}
  </div>
</div>
