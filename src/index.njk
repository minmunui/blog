---
layout: layouts/base.njk
title: Home
---
<section class="grid items-center gap-6 pb-8 pt-6 md:py-10 px-1 max-w-[1440px] mx-auto">
  <div class="flex w-full flex-col items-center gap-2">
    <h1 class="text-3xl font-extrabold leading-tight tracking-tighter md:text-4xl text-center">
      최근 포스트
    </h1>
    <p class="max-w-[700px] text-lg text-muted-foreground text-center">
      잡담, 프로젝트, 기술 등등.
    </p>
  </div>
  
  <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 mt-8">
    {% for post in collections.posts | reverse %}
      <div class="rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="p-6 flex flex-col gap-4 h-full">
            <div class="flex flex-col gap-1">
                 {% if post.data.thumbnail %}
                    <div class="aspect-video w-full overflow-hidden rounded-md mb-4 bg-muted">
                        <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" class="object-cover w-full h-full transition-transform hover:scale-105" />
                    </div>
                 {% else %}
                    <div class="aspect-video w-full overflow-hidden rounded-md mb-4 bg-muted flex items-center justify-center text-muted-foreground">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-image"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/></svg>
                    </div>
                 {% endif %}
                 <h3 class="text-2xl font-semibold leading-none tracking-tight">
                    <a href="{{ post.url | url }}" class="hover:underline">{{ post.data.title }}</a>
                 </h3>
                 <p class="text-sm text-muted-foreground">{{ post.date | dateReadable }}</p>
            </div>
            
            {% if post.data.description %}
            <p class="text-muted-foreground">
                {{ post.data.description }}
            </p>
            {% endif %}

            <div class="mt-auto pt-4 flex gap-2">
                 {% for tag in post.data.tags %}
                    {% if tag != "posts" %}
                        <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">{{ tag }}</span>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
      </div>
    {% endfor %}
  </div>
</section>
